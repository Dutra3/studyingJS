let calc = {
    mult: (x,y) => {
        return x * y;
    },

    sum: (x,y) => {
        return x + y;
    },

    minus: (x,y) => {
        return x - y;
    },

    div: (x,y) => {
        return x / y;
    }
}

module.exports = calc;